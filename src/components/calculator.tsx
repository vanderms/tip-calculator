import Form from './form';

export default function Calculator() {
  return (
    <article className="nv-calculator">
      <h1 className="nv-screen-reader-only">Tip calculator</h1>
      <Form />
    </article>
  );
}
